name: Secrets-workflow
on: push
env:
  Name: Secrets
jobs:
  secrets-pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Secrets
        run: echo :print the secrets values of ${{ secrets.github-actions }}"
    
