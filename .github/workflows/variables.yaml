name: demo
on: push
env:
  NAME: Anil
jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Print variable
        run: echo "I am $NAME"
      - name: github variables
        run: echo "${{ vars.MY_VAR }}" // calling variables from  github variables list
